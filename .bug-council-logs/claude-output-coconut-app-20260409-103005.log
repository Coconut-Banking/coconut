PR_NUMBER=56

---

## Bug Council Audit Complete — 2026-04-09

**5 bugs reported → 5 verified → 5 fixed → 0 deferred → 0 disproved**

### Fixed

| ID | Sev | What | File(s) |
|----|-----|------|---------|
| BUG-NATIVE-1 | P0 | Hardcoded Clerk test key as env var fallback in `app.config.js` — bundled in every build | `app.config.js` |
| BUG-NAV_AUTH-1 | P1 | `.json()` called before `.ok` check in `TerminalTokenProvider` — HTML 500 body caused unrelated SyntaxError | `app/_layout.tsx` |
| BUG-NATIVE-2 | P1 | Null token after retry → empty `Authorization: ""` header → generic 401; now throws early with actionable message | `app/_layout.tsx` |
| BUG-API-1 | P1 | `saveAssignments` ignored `.ok`, so `computeSummary()` ran unconditionally — user advanced to summary with unsaved data | `hooks/useReceiptSplit.ts`, `app/(tabs)/receipt.tsx` |
| BUG-API-2 | P2 | `confirmItems` empty `catch` block swallowed all errors silently — user got zero feedback on failure | `hooks/useReceiptSplit.ts`, `app/(tabs)/receipt.tsx` |

### Patterns (recurring — now in `.cursor/rules/common-bugs.mdc`)
- `.ok`/`.json()` order: 7th+ occurrence across the codebase
- Empty catch blocks + silent API failure

### Validation
- `npx tsc --noEmit`: same 8 pre-existing errors, no new errors
- 3 fix commits on `fix/bug-council-20260409`, tagged `bug-council-20260409`
- PR: https://github.com/Coconut-Banking/coconut-app/pull/56
