Done. Here's a summary of how each conflict was resolved:

| File | Resolution |
|------|-----------|
| `app/(auth)/sign-in.tsx` | Took upstream's "already signed in" redirect logic and new SafeAreaView layout. Dropped our console.log/console.error statements. |
| `app/(auth)/sign-up.tsx` | Took upstream's redesigned layout with SafeAreaView, ScrollView, and new brand section. |
| `app/(tabs)/insights.tsx` | Took upstream's structure: !linked check first, then loading check, using theme colors instead of hardcoded values. |
| `app/(tabs)/receipt.tsx` | Combined both: took upstream's `recalcSubtotal` from items **and** kept our bug fix to include extras in the total calculation. |
| `app/(tabs)/shared.tsx` | Deleted — upstream replaced it with a `shared/` directory structure. |
| `app/_layout.tsx` | Took upstream's version, removing `TerminalTokenProvider` and conditional `tokenCache`. |
| `hooks/useTransactions.ts` | Kept our `isCancelled()` (fetchId-based pattern) since upstream's `cancelled` variable was undefined in this context. |
