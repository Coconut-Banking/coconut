# Coconut mobile UI reference

The old standalone prototype folder was removed during repo cleanup.

Use the active Expo app as the source of truth:

- **Implementation notes / mapping:** `coconut-app/docs/UI_PROTOTYPE_SOURCE.md`
- **Runtime design tokens:** `coconut-app/lib/theme.ts` (`darkUI`, `prototype`)

When changing mobile UI, follow those references directly so the shipped app and design guidance stay in sync.
